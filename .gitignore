# rust
/target

# IDE (RustRover / IntelliJ)
.idea/

# logs
*.log

# secrets / local env
.env

# claude — глобальный gitignore игнорирует .claude/, но skills/ — часть
# проекта (наши паттерны, ROADMAP-based знания), их хотим коммитить.
!.claude/
.claude/settings.local.json
